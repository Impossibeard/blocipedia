<h1> New Wiki </h1>

<div class="row">
  <div class="col-md-8">
    <p>Guidelines for wikis:</p>
    <ul>
      <li>External links should be kept directly relevant to the article.</li>
      <li>Don't plagiarize and give credit where it is due.</li>
      <li>Avoid using disclaimers in articles as wikis are generally full of spoilers. </li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
      </div>
      <%= render partial: 'collaborators/collaborator', locals: { f: f, wiki: @wiki } %>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-primary' %>
      </div>
      <%= render partial: 'wikis/form', locals: { f: f } %>
    <% end %>
  </div>
</div>
